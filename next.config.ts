import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel: default Next.js deployment works without static export.
  // Uncomment for fully static HTML export only:
  // output: "export",
};

export default nextConfig;
